# Core dependencies for LooGLE Benchmark
openai>=1.0.0
datasets>=2.14.0

# Optional dependencies for running vLLM server (if needed locally)
# vllm>=0.2.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0 